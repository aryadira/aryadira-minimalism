import { Interaction, Introduce } from "../custom";

const About = () => {
  return (
    <section id='about' className='w-full'>
      <Introduce />
      <Interaction />
    </section>
  );
};

export default About;
